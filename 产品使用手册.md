# SilverM-quant 产品使用手册

## 这个项目是什么

一个 A 股量化交易辅助系统。它帮你做三件事：

1. **每天收盘后自动扫描全市场**，找出符合 7 种策略的买入/卖出信号
2. **用 AI 多 Agent 辩论**分析某只股票值不值得买（多头 vs 空头 vs 中性三方辩论）
3. **回测验证**策略在历史数据上的表现

它**不会自动下单**。最终买卖决策由你自己做，你需要手动去券商 APP 操作。

---

## 日常使用流程

```
每天 16:00 后（收盘后数据更新完毕）

1. 打开 http://127.0.0.1:5001
2. 去「数据更新」页面 → 确认数据源是 Baostock → 选"日线数据" → 点「🚀 开始更新」
   （约 8-10 分钟下载全市场当天数据）
3. 更新完成后，点「📡 扫描信号」按钮（约 1 分钟完成全市场扫描）
4. 回到「信号」页面，查看今日买入/卖出信号
5. 对感兴趣的股票，去「Agent分析」页面输入代码，获取 AI 多维度分析
6. 自己决定是否买入，去券商 APP 下单
```

---

## 各页面功能说明

### 首页 (/)

概览面板，显示：
- 当前持仓数量和盈亏
- 今日买入信号数
- 资金曲线（需要有 portfolio_daily 数据）
- 策略对比（各策略的收益对比）

### 交易信号 (/signals)

展示最新一天的信号扫描结果：
- **买入信号**：B1、B2、BLK、DL、DZ30、SCB、BLKB2 共 7 种策略
- **卖出信号**：S1（满仓/半仓）、跌破多空线、止损
- 每只股票显示触发了哪些策略、评分多少

### 持仓管理 (/positions)

你的当前持仓和历史交易记录。**需要手动维护**：
- 买入后在数据库 `positions` 表插入记录
- 卖出后更新 status 为 'sold'

### 多信号共振 (/multi-signal-resonance)

找出同时被 2 个以上策略看好的股票 — 多策略共振通常更可靠。

### 回测 Dashboard (/backtest-dashboard)

对策略进行历史回测验证：
1. 选择策略（目前有"策略模板"）
2. 设置日期范围和股票池
3. 点击运行，查看收益率、夏普比率、最大回撤等指标

### Agent 智能分析 (/agent)

输入股票代码，AI 多 Agent 系统会：
1. 获取该股票的行情数据和新闻
2. 多头 Agent 找买入理由
3. 空头 Agent 找风险点
4. 三方辩论得出结论
5. 风险管理 Agent 评估风险等级
6. 最终输出：买入/观望/卖出 建议 + 置信度

### 数据更新 (/data-update)

管理数据下载：
- **数据源切换**：Baostock（免费）/ Tushare（需 token，数据更全）
- **支持的更新类型**：

| 类型 | Baostock | Tushare | 说明 |
|------|----------|---------|------|
| 股票信息 | ✅ | ✅ | 股票列表、名称、行业 |
| 日线数据 | ✅ | ✅ | OHLCV 行情 |
| 每日指标 | ❌ | ✅ | PE/PB/换手率等 |
| 复权因子 | ❌ | ✅ | 前复权/后复权 |
| 交易日历 | ❌ | ✅ | 交易日/休市日 |
| 指数数据 | ❌ | ✅ | 上证/深证/创业板指数 |
| 财务数据 | ❌ | ✅ | 利润表/资产负债表/现金流 |

---

## 7 种买入策略简介

| 策略 | 核心逻辑 |
|------|----------|
| B1 | KDJ J值<13 + MACD多头 + 趋势线多头 + 综合评分≥8 |
| B2 | MACD多头 + 趋势线多头 + B2评分≥8（不要求J值低） |
| BLK | 暴力K线（大阳线）+ 趋势线多头 |
| BLKB2 | 暴力K + 倍量柱 + J拐头向上 + B2评分≥8 |
| DL | 地量条件（缩量到极致后放量） |
| DZ30 | 长期KD≥80 + 短期KD≤30 + 趋势多头 + 有倍量柱 |
| SCB | 沙尘暴策略（地量+暴力K组合信号） |

卖出信号：
- **S1**：基于高位放量阴线的评分系统（>10分全卖，>5分半卖）
- **跌破多空线**：价格跌破20日均线
- **止损**：亏损超过设定比例（默认5%）

---

## 启动和停止

```bash
cd /Users/liuhui/SilverM-quant

# 启动
source .venv/bin/activate
python -c "from dashboard.app import app; app.run(host='0.0.0.0', port=5001)"

# 后台启动
python -c "from dashboard.app import app; app.run(host='0.0.0.0', port=5001)" &

# 停止
pkill -f "dashboard.app"
```

访问地址：http://127.0.0.1:5001

---

## 命令行工具

```bash
# 信号扫描（每天收盘后运行）
python signals/scan_signals_v2.py --date 20250521 --workers 4

# 单次回测
python backtest/strategy_backtest/run_backtest.py -s 600000 --start 20250101 --end 20250520

# 数据流水线（一键更新数据+扫描信号）
python scripts/workflow_scheduler.py --run --pipeline daily --force-source baostock

# 查看流水线状态
python scripts/workflow_scheduler.py --status
```

---

## 配置文件

### .env（API 密钥）

```
DEEPSEEK_API_KEY=sk-xxx          # Agent分析用的 LLM
DEEPSEEK_BASE_URL=http://127.0.0.1:8317/v1  # 本地代理地址
LLM_MODEL=gpt-5.5               # 模型名称
TUSHARE_TOKEN=                   # 可选，tushare数据源需要
```

### config/settings.py（系统参数）

- `DEFAULT_INITIAL_CASH`：默认初始资金（10万）
- `DEFAULT_STOP_LOSS`：默认止损比例（5%）
- `DEFAULT_MAX_POSITIONS`：最大持仓数（10只）
- `DATA_SOURCE`：默认数据源

---

## 项目不做什么

- ❌ 不自动下单（没有券商接口）
- ❌ 不自动管理持仓（需要手动记录买卖）
- ❌ 不保证盈利（策略仅供参考）
- ❌ 不提供实时行情（收盘后批量更新）

---

## 典型工作流示例

### 场景：发现并验证一只股票

1. 下午 4 点，运行数据更新 + 信号扫描
2. 在「交易信号」页面看到 000700（模塑科技）同时触发 B2+BLK+SCB+BLKB2 四个买入信号
3. 去「多信号共振」确认它是今天共振最强的股票
4. 去「Agent分析」输入 000700，AI 给出"观望，置信度0.62"的建议
5. 去「回测」页面，用策略模板对 000700 跑 2025 年回测，看历史表现
6. 综合判断后决定明天开盘买入，去券商 APP 下单
7. 买入后在 positions 表记录持仓

### 场景：每日监控持仓

1. 运行信号扫描
2. 查看「交易信号」页面，关注持仓股票是否出现卖出信号（S1/跌破多空线/止损）
3. 如果出现卖出信号，去券商 APP 卖出
4. 更新 positions 表
